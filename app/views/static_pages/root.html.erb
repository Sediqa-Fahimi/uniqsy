<% if logged_in? %>
    <script type="text/javascript">
        
        window.currentUser = <%= render("api/users/user.json.jbuilder",user: current_user).html_safe %>
        window.cartItems = <%= render("api/cartitems/cartitem.json.jbuilder", cartitems: current_user.cartitems ).html_safe %>

    </script>
<% end %>

<main id="root"></main>